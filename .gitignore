# data (large — distributed in the hackathon bundle, not committed)
candidates.jsonl
candidates.jsonl.gz

# generated outputs
submission.csv
_audit*.py

# python
__pycache__/
*.pyc
.pytest_cache/
.ipynb_checkpoints/
.venv/
venv/

# os / editor
.DS_Store
.idea/
.vscode/

# NOTE: artifacts/semantic_scores.tsv IS committed (a precomputed artifact the
# ranking step depends on). Regenerable via precompute_embeddings.py.
